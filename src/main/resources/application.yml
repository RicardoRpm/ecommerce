#CONFIGURA��O DO SPRING

#CONFIGURA��O DA BASE DE DADOS
spring:
  thymeleaf:
    prefix:
      classpath: templates
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/e_comerce?useTimezone=true&serverTimezone=UTC
    username: root
    password:
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
    show-sql: true